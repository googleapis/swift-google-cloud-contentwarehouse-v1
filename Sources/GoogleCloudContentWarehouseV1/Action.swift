// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Represents the action triggered by Rule Engine when the rule is true.
public struct Action: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// ID of the action. Managed internally.
  public var actionId: Swift.String = Swift.String()

  public var action: OneOf_Action? = nil

  /// Initialize a new instance of `Action`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Action().with { $0.actionId = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case actionId = "actionId"
    case accessControl = "accessControl"
    case dataValidation = "dataValidation"
    case dataUpdate = "dataUpdate"
    case addToFolder = "addToFolder"
    case publishToPubSub = "publishToPubSub"
    case removeFromFolderAction = "removeFromFolderAction"
    case deleteDocumentAction = "deleteDocumentAction"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.actionId = try container.decode(Swift.String.self, forKey: .actionId)

    var action: OneOf_Action? = nil
    let actionCheckAndSet = {
      if action != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'action'"))
      }
      action = $0
    }
    if let accessControl = try container.decodeIfPresent(
      AccessControlAction?.self, forKey: .accessControl)
    {
      try actionCheckAndSet(.accessControl(accessControl))
    }
    if let dataValidation = try container.decodeIfPresent(
      DataValidationAction?.self, forKey: .dataValidation)
    {
      try actionCheckAndSet(.dataValidation(dataValidation))
    }
    if let dataUpdate = try container.decodeIfPresent(DataUpdateAction?.self, forKey: .dataUpdate) {
      try actionCheckAndSet(.dataUpdate(dataUpdate))
    }
    if let addToFolder = try container.decodeIfPresent(
      AddToFolderAction?.self, forKey: .addToFolder)
    {
      try actionCheckAndSet(.addToFolder(addToFolder))
    }
    if let publishToPubSub = try container.decodeIfPresent(
      PublishAction?.self, forKey: .publishToPubSub)
    {
      try actionCheckAndSet(.publishToPubSub(publishToPubSub))
    }
    if let removeFromFolderAction = try container.decodeIfPresent(
      RemoveFromFolderAction?.self, forKey: .removeFromFolderAction)
    {
      try actionCheckAndSet(.removeFromFolderAction(removeFromFolderAction))
    }
    if let deleteDocumentAction = try container.decodeIfPresent(
      DeleteDocumentAction?.self, forKey: .deleteDocumentAction)
    {
      try actionCheckAndSet(.deleteDocumentAction(deleteDocumentAction))
    }
    self.action = action
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.actionId, forKey: .actionId)

    if let choice = self.action {
      switch choice {
      case .accessControl(let value):
        try container.encode(value, forKey: .accessControl)
      case .dataValidation(let value):
        try container.encode(value, forKey: .dataValidation)
      case .dataUpdate(let value):
        try container.encode(value, forKey: .dataUpdate)
      case .addToFolder(let value):
        try container.encode(value, forKey: .addToFolder)
      case .publishToPubSub(let value):
        try container.encode(value, forKey: .publishToPubSub)
      case .removeFromFolderAction(let value):
        try container.encode(value, forKey: .removeFromFolderAction)
      case .deleteDocumentAction(let value):
        try container.encode(value, forKey: .deleteDocumentAction)
      }
    }
  }

  public enum OneOf_Action: Codable, Equatable, Sendable {
    /// Action triggering access control operations.
    indirect case accessControl(AccessControlAction?)
    /// Action triggering data validation operations.
    indirect case dataValidation(DataValidationAction?)
    /// Action triggering data update operations.
    indirect case dataUpdate(DataUpdateAction?)
    /// Action triggering create document link operation.
    indirect case addToFolder(AddToFolderAction?)
    /// Action publish to Pub/Sub operation.
    indirect case publishToPubSub(PublishAction?)
    /// Action removing a document from a folder.
    indirect case removeFromFolderAction(RemoveFromFolderAction?)
    /// Action deleting the document.
    indirect case deleteDocumentAction(DeleteDocumentAction?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.contentwarehouse.v1.Action"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
